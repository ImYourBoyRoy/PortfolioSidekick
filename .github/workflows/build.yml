# Portfolio Sidekick CI — floating latest-stable toolchain policy
#
# GitHub official actions: pin latest major tag (v6/v7/v8) + check-latest where supported.
# Third-party actions: @master when the repo publishes master; else latest major version tag
#   (swatinem/rust-cache@v2, android-actions/setup-android@v4 — no reliable @master).
# Node: sidekick/.node-version major line + actions/setup-node check-latest:true.
# Rust: rust-toolchain.toml channel=stable + dtolnay/rust-toolchain@master toolchain:stable.
# Android deps: upgrade-android-build-deps.mjs (Gradle, AGP, Kotlin, AndroidX, Cordova).
# Audit locally: node sidekick/scripts/upgrade-ci-toolchain.mjs [--check]

name: Compile & Package Multi-Platform Releases

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-desktop:
    name: Build Desktop Release (Tauri)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: sidekick/.node-version
          check-latest: true
          cache: npm
          cache-dependency-path: sidekick/package-lock.json

      - name: Verify desktop toolchain policy
        run: node sidekick/scripts/upgrade-ci-toolchain.mjs --check

      - name: Install Linux Tauri Dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev

      - name: Setup Rust (stable — tracks rust-toolchain.toml channel)
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          targets: aarch64-apple-darwin,x86_64-apple-darwin,x86_64-pc-windows-msvc,x86_64-unknown-linux-gnu

      - name: Rust Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: |
            sidekick/src-tauri -> sidekick/src-tauri/target
          shared-key: portfolio-sidekick
          cache-bin: true

      - name: Install Frontend Dependencies & Build
        run: |
          cd sidekick
          npm ci
          npm run lint
          npm run test
          npm run build

      - name: Build Tauri Desktop Bundle
        run: |
          cd sidekick
          npm run tauri:build

      - name: Package Windows Artifact
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $releaseDir = "sidekick/src-tauri/target/release"
          $portableExe = "$releaseDir/portfolio-sidekick.exe"
          if (-not (Test-Path $portableExe)) {
            $exe = Get-ChildItem -Path $releaseDir -Filter "portfolio-sidekick.exe" | Select-Object -First 1
            if (-not $exe) { throw "No portable Windows EXE found at target/release/portfolio-sidekick.exe" }
            $portableExe = $exe.FullName
          }
          Copy-Item $portableExe ./PortfolioSidekick-Windows.exe

      - name: Package macOS App Bundle
        if: matrix.os == 'macos-latest'
        run: |
          set -euo pipefail
          BUNDLE_DIR="sidekick/src-tauri/target/release/bundle"
          APP_DIR="$BUNDLE_DIR/macos/Portfolio Sidekick.app"
          if [ -d "$APP_DIR" ]; then
            zip -r ./PortfolioSidekick-MacOS.zip "$APP_DIR"
            exit 0
          fi
          APP=$(find "$BUNDLE_DIR" -type d -name "*.app" | head -1)
          if [ -n "$APP" ]; then
            zip -r ./PortfolioSidekick-MacOS.zip "$APP"
            exit 0
          fi
          DMG=$(find "$BUNDLE_DIR" -maxdepth 2 -type f -name "*.dmg" | head -1)
          if [ -n "$DMG" ]; then
            cp "$DMG" ./PortfolioSidekick-MacOS.dmg
            exit 0
          fi
          echo "No macOS .app or .dmg bundle found under $BUNDLE_DIR"
          find "$BUNDLE_DIR" -maxdepth 3 || true
          exit 1

      - name: Package Linux Binary
        if: matrix.os == 'ubuntu-latest'
        run: |
          BIN=$(find sidekick/src-tauri/target/release/bundle -type f \( -name "*.AppImage" -o -name "portfolio-sidekick" -o -name "Portfolio Sidekick" \) | head -1)
          if [ -z "$BIN" ]; then
            BIN=sidekick/src-tauri/target/release/portfolio-sidekick
          fi
          cp "$BIN" ./PortfolioSidekick-Linux
          tar -czvf ./PortfolioSidekick-Linux.tar.gz PortfolioSidekick-Linux

      - name: Upload Windows Artifact
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v7
        with:
          name: desktop-windows-latest
          path: PortfolioSidekick-Windows.exe

      - name: Upload macOS Artifact
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v7
        with:
          name: desktop-macos-latest
          path: |
            PortfolioSidekick-MacOS.zip
            PortfolioSidekick-MacOS.dmg
          if-no-files-found: error

      - name: Upload Linux Artifact
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v7
        with:
          name: desktop-ubuntu-latest
          path: PortfolioSidekick-Linux.tar.gz

  build-android:
    name: Build Android Mobile APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Setup Java JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '24'
          check-latest: true

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android compile SDK platform (API 37.0)
        run: sdkmanager "platforms;android-37.0"

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: sidekick/.node-version
          check-latest: true
          cache: npm
          cache-dependency-path: sidekick/package-lock.json

      - name: Install Frontend Dependencies & Build React App
        run: |
          cd sidekick
          npm ci
          npm run lint
          npm run test
          npm run build

      - name: Initialize Capacitor Android Platform
        run: |
          cd sidekick
          npx cap sync
          npx cap add android || echo "Android platform already added"
          npx cap sync android

      - name: Inject Custom Android Launcher Icons
        run: |
          mkdir -p sidekick/android/app/src/main/res/mipmap-mdpi
          mkdir -p sidekick/android/app/src/main/res/mipmap-hdpi
          mkdir -p sidekick/android/app/src/main/res/mipmap-xhdpi
          mkdir -p sidekick/android/app/src/main/res/mipmap-xxhdpi
          mkdir -p sidekick/android/app/src/main/res/mipmap-xxxhdpi
          cp assets/android/icon-mdpi.png sidekick/android/app/src/main/res/mipmap-mdpi/ic_launcher.png
          cp assets/android/icon-mdpi.png sidekick/android/app/src/main/res/mipmap-mdpi/ic_launcher_round.png
          cp assets/android/icon-hdpi.png sidekick/android/app/src/main/res/mipmap-hdpi/ic_launcher.png
          cp assets/android/icon-hdpi.png sidekick/android/app/src/main/res/mipmap-hdpi/ic_launcher_round.png
          cp assets/android/icon-xhdpi.png sidekick/android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
          cp assets/android/icon-xhdpi.png sidekick/android/app/src/main/res/mipmap-xhdpi/ic_launcher_round.png
          cp assets/android/icon-xxhdpi.png sidekick/android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
          cp assets/android/icon-xxhdpi.png sidekick/android/app/src/main/res/mipmap-xxhdpi/ic_launcher_round.png
          cp assets/android/icon-xxxhdpi.png sidekick/android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
          cp assets/android/icon-xxxhdpi.png sidekick/android/app/src/main/res/mipmap-xxxhdpi/ic_launcher_round.png
          cp assets/android/icon-foreground-mdpi.png sidekick/android/app/src/main/res/mipmap-mdpi/ic_launcher_foreground.png
          cp assets/android/icon-foreground-hdpi.png sidekick/android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png
          cp assets/android/icon-foreground-xhdpi.png sidekick/android/app/src/main/res/mipmap-xhdpi/ic_launcher_foreground.png
          cp assets/android/icon-foreground-xxhdpi.png sidekick/android/app/src/main/res/mipmap-xxhdpi/ic_launcher_foreground.png
          cp assets/android/icon-foreground-xxxhdpi.png sidekick/android/app/src/main/res/mipmap-xxxhdpi/ic_launcher_foreground.png
          mkdir -p sidekick/android/app/src/main/res/mipmap-anydpi-v26
          cat << 'EOF' > sidekick/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@color/ic_launcher_background"/>
              <foreground android:drawable="@mipmap/ic_launcher_foreground"/>
          </adaptive-icon>
          EOF
          cat << 'EOF' > sidekick/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@color/ic_launcher_background"/>
              <foreground android:drawable="@mipmap/ic_launcher_foreground"/>
          </adaptive-icon>
          EOF
          mkdir -p sidekick/android/app/src/main/res/values
          rm -f sidekick/android/app/src/main/res/values/colors.xml
          rm -f sidekick/android/app/src/main/res/values/ic_launcher_background.xml
          cat << 'EOF' > sidekick/android/app/src/main/res/values/ic_launcher_background.xml
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <color name="ic_launcher_background">#070c12</color>
          </resources>
          EOF

      - name: Inject Native Robinhood Plugin & Security Hardening
        run: |
          chmod +x sidekick/scripts/inject-android-native.sh
          sidekick/scripts/inject-android-native.sh

      - name: Patch Android versionCode and signing config
        run: node sidekick/scripts/patch-android-build.mjs

      - name: Upgrade Android build deps to latest stable
        run: node sidekick/scripts/upgrade-android-build-deps.mjs

      - name: Prepare Android home for debug keystore cache
        run: mkdir -p ~/.android

      - name: Cache Android debug keystore (consistent sideload signature fallback)
        uses: actions/cache@v5
        with:
          path: ~/.android
          key: sidekick-android-home-v1
          restore-keys: |
            sidekick-android-home-v1

      - name: Compile Android APK via Gradle
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          SIDEKICK_ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          SIDEKICK_ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          SIDEKICK_ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          cd sidekick/android
          chmod +x gradlew
          if [ -n "$ANDROID_KEYSTORE_BASE64" ]; then
            mkdir -p keystore
            echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > keystore/sidekick-release.jks
            export SIDEKICK_ANDROID_KEYSTORE="$(pwd)/keystore/sidekick-release.jks"
            ./gradlew assembleRelease
            cp app/build/outputs/apk/release/app-release.apk ../../PortfolioSidekick-Android.apk
          else
            echo "No ANDROID_KEYSTORE_BASE64 secret — building debug APK with cached debug.keystore"
            ./gradlew assembleDebug
            cp app/build/outputs/apk/debug/app-debug.apk ../../PortfolioSidekick-Android.apk
          fi

      - name: Upload Mobile APK Artifact
        uses: actions/upload-artifact@v7
        with:
          name: mobile-apk
          path: PortfolioSidekick-Android.apk

  publish-releases:
    name: Publish Assets to GitHub Release
    needs: [build-desktop, build-android]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v8
        with:
          path: release-assets
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@master
        with:
          files: |
            release-assets/PortfolioSidekick-Windows.exe
            release-assets/PortfolioSidekick-MacOS.zip
            release-assets/PortfolioSidekick-MacOS.dmg
            release-assets/PortfolioSidekick-Linux.tar.gz
            release-assets/PortfolioSidekick-Android.apk
          fail_on_unmatched_files: false
          name: Portfolio Sidekick Release ${{ github.ref_name }}
          tag_name: ${{ github.ref }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
